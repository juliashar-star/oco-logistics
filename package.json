{
  "name": "oco-logistics",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "overrides": {
    "postcss": "8.5.15"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/integrations/*"
  ],
  "scripts": {
    "predev": "node scripts/free-port.js 3000",
    "dev": "npm run dev -w @oco/web",
    "build": "npm run build -w @oco/web",
    "lint": "npm run lint -w @oco/web",
    "db:generate": "prisma generate --schema packages/db/prisma/schema.prisma",
    "db:migrate": "prisma migrate dev --schema packages/db/prisma/schema.prisma",
    "db:push": "prisma db push --schema packages/db/prisma/schema.prisma",
    "db:backup": "bash infra/backups/backup.sh",
    "db:restore": "bash infra/backups/restore.sh",
    "docker:up": "docker compose -f infra/docker-compose.yml up -d",
    "docker:down": "docker compose -f infra/docker-compose.yml down",
    "test": "node --import tsx --test tests/*.test.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.scripts.json && npm run typecheck -w @oco/core && npm run typecheck -w @oco/web",
    "carrier:drift": "node --env-file=.env node_modules/tsx/dist/cli.mjs scripts/check-carrier-drift.ts"
  },
  "devDependencies": {
    "prisma": "^6.9.0",
    "tsx": "^4.19.4",
    "typescript": "^5"
  }
}
