# Roadmap — OCO Logistics (MVP)

План по вехам. Каждая веха — самостоятельный кусок, который можно показать и проверить.
Сроки указаны для работы в одиночку с Cursor (ориентир — 6–8 недель). Cursor использует
этот файл, чтобы понимать порядок работ и не забегать вперёд.

Правило: **не начинаем следующую веху, пока предыдущая не проверена руками.**

---

## M0 · Каркас (≈ неделя 1)
Цель: пустой, но рабочий скелет проекта.
- Создать структуру папок (см. ARCHITECTURE.md).
- Поднять Next.js + Prisma + PostgreSQL в Docker Compose локально.
- Открывается пустая главная страница, приложение подключается к базе.
- Готов `.env.example`, базовая авторизация (заглушка входа).
**Готово, когда:** локально открывается кабинет, база подключена, `docker compose up` работает.
Истории: US-1.1, US-1.2 (база).

## M1 · Ядро «Новый заказ» (≈ недели 2–3)
Цель: главная ценность продукта работает на тестовом APIShip.
- Слой интеграций APIShip (sandbox): расчёт тарифов, список ПВЗ.
- Форма посылки + таблица вариантов.
- Decision Engine: «Быстро/Дёшево/Оптимально».
- Создание отправления → трек-номер.
- Сохранение всех вариантов и отправления в базу (актив с первого дня).
**Готово, когда:** ввёл посылку → увидел варианты → создал заказ → получил трек, всё за < 3 минут.
Истории: US-2.1, US-2.2, US-3.1–US-3.4.

## M2 · Отправления и трекинг (≈ недели 4–5)
Цель: контроль над уже созданными отправлениями.
- Список отправлений с фильтрами и поиском.
- Периодический опрос статусов APIShip, сохранение событий трекинга.
- Фиксация фактических дат, возвратов, отмен.
- Экспорт в CSV.
**Готово, когда:** все заказы видны со статусами, статусы обновляются, история событий сохраняется.
Истории: US-4.1–US-4.4.

## M3 · Данные и дашборд (≈ недели 6–7)
Цель: продукт начинает «умнеть» и показывать пользу.
- Carrier Score по накопленным данным.
- «Оптимально» учитывает Carrier Score.
- Дашборд: заказы, расходы, экономия, график.
- Онбординг на пустых экранах.
**Готово, когда:** дашборд показывает реальные цифры, рейтинг влияет на рекомендации.
Истории: US-5.1–US-5.3.

### Carrier Picker

- Carrier Picker Task 3 (CarrierScore stub layer, score.ts): ✅ сделано — обнаружено реализованным при инвентаризации 2026-07-05 (коммит 93e5172), задокументировано в DECISIONS.md. P0-AN2 (реальные данные вместо hasData:false) остаётся отдельной будущей задачей маховика.
- P0-KN4 (матчинг-движок): ✅ верифицирован и расширен — единый принцип показа полного рынка с статусом подключения для всех профилей P1–P7 (2026-07-06, см. DECISIONS.md). Открыто: кнопка заявки на подключение (БД + email-уведомление), ocoConnectionEstimate (заполнить после первых реальных подключений).

### P0-KN3 — Публичное сравнение перевозчиков (методика)
- Методика зафиксирована: docs/OCO_carrier_rating_spec_1.md (2026-07-05)
- KN3-1 (данные/схема источников фактов в registry.ts): не начато
- KN3-2 (детерминированный вывод тегов «подходит для Pх»): не начато
- KN3-3 (публичная страница «Сравнение»): не начато — 🔴 требует юриста перед публикацией
- KN3-4 (страница «Методика»): не начато — 🔴 требует юриста перед публикацией
- KN3-5 (healthStatus в выдаче): не начато
- KN3-6 (согласование healthStatus с rank.ts): не начато — открытый хвост, не терять
- KN3-7 (data-слой, Carrier Score с гейтом/сглаживанием): позже, после ~10 активных продавцов

---

## Открытые хвосты (сводка на 2026-07-07)

Список отслеживаемых мелких и средних незакрытых пунктов — держать
актуальным, не терять между сессиями.

1. **Заявка на подключение — UI**: бэкенд готов (модель, роут, email),
   UI-кнопка/бейдж в carrier-picker-dashboard-form.tsx ещё не сделаны.
2. **Флаг «хрупкое» → реальное создание отправления**: сейчас
   needsFragile влияет только на рекомендацию; перед M4 нужно прокинуть
   rupost.fragile в /new-order → APIShip extraParam.
3. **Упоминания APIShip в клиентском UI**: вкладка «Подбор перевозчика»
   (инструкция входа в кабинет APIShip) и «Настройки → Подключение»
   (логин/пароль APIShip) противоречат messaging модели F. Полностью
   решится с прямыми адаптерами (P0-EXE1); рассмотреть отдельно, можно
   ли улучшить формулировки раньше.
4. **Формулировка carrierContractEstimate**: «требует уточнения у
   перевозчика» в UI должна стать явной инструкцией вида «Срок
   заключения договора — уточните напрямую у [название перевозчика]».
5. **Registry v2**: расширение реестра сверх 12 перевозчиков — кандидаты
   TopDelivery (td), КГТ, Ozon Доставка (ozonlog), Магнит Пост (magnit),
   Пони-экспресс (pony), Major Express (major). Требуют анализа перед
   кодированием (спецификация → код).
6. **Полная анкета подбора**: region/priority/method захардкожены в
   recommend.ts ("all_russia"/"reliable"/"both"), нет UI-полей выбора.
   Плюс отложенные capability-флаги: rupost.TransportMode (скорость),
   yataxi.cargo_options.thermobag (термо) — намеренно не сделаны сейчас
   (реализован только rupost.fragile), вернуться при расширении.
7. **Юридический вопрос — температурный режим/скоропорт**: перевозка
   еды/лекарств регулируется (СанПиН, лицензирование); нужен ответ
   юриста до включения любого термо-флага.
8. **Per-carrier лимиты веса**: единый глобальный порог (30кг/120см)
   не отражает реальные лимиты разных перевозчиков (Яндекс Экспресс
   ≤20кг, Почта ≤20кг стандартной посылкой, СДЭК фикс. цена ≤30кг).
   Рассмотреть переход на per-carrier weightLimits (уже есть в схеме).
9. **P1–P4 profile-fit**: не выводятся из фактов, остаются статичным
   полем profiles в registry.ts (осознанное сужение KN3-2). Вернуться
   перед M4 — решить, оставлять ли навсегда или дорабатывать.
10. **Право на возражение (KN3)**: плейсхолдер-текст «сообщить о
    неточности» на /carrier-comparison и /carrier-comparison/methodology;
    теперь есть privacy@useoco.ru — можно подключить реальную форму/email.
11. 🔴 **Юридический гейт на /carrier-comparison**: страница физически
    заперта флагом ENABLE_CARRIER_COMPARISON_PAGE=false; публикация
    требует ответа юриста по трём пунктам §9 методики KN3.
12. **Чистка notes в реестре**: субъективные формулировки («самая
    развитая», «рекордное») нарушают собственную методику нейтральности,
    почистить перед любым публичным использованием поля notes.
13. **prose-slate**: проверить, подключён ли Tailwind Typography plugin —
    класс используется на странице методики KN3, визуально не
    проверялось.
14. **Полноценная аналитика в дашборде**: текущий /dashboard слишком
    скудный (4 карточки + топ-перевозчиков). Нужны графики, динамика,
    фильтры, разбивка по товарам/службам, среднее время доставки,
    отклонения, возвраты, отказы. Отдельная дизайн-сессия, не мелкая
    правка — данные для этого уже частично заложены (TrackingEvent,
    ADR по аналитике из 2026-06).
15. **db:backup/db:restore**: скрипты написаны на bash, Git Bash
    недоступен на машине разработки. Нужна кросс-платформенная
    альтернатива (например, через docker compose exec pg_dump, раз
    Postgres поднят в Docker).
16. **Unisender Go**: проверить доставку на julia.abr@bk.ru после снятия
    temporary_unavailable (ожидалось ~10.07.2026).

## M4 · Боевой запуск (≈ неделя 8)
Цель: можно пускать реальных клиентов — по закону и безопасно.
- Развернуть на российском сервере (см. инструкцию по подключению сервера).
- Домен + HTTPS, ежедневные бэкапы базы.
- Переключить APIShip на боевой ключ.
- Разместить Политику обработки ПДн и согласие; зарегистрироваться оператором ПДн в РКН;
  добавить IT-ОКВЭД в ИП.
- Настроить периодический автоматический опрос статусов APIShip (cron/worker) вместо ручной
  кнопки — US-4.2.
- Подключить первые 1–2 пилотных бренда (часть заказов сначала можно вести вручную — concierge).
- [ ] Единый экран регистрации (autosignup APIShip + white-label) — перед запуском
- [ ] ЗАРАНЕЕ: запрос в поддержку APIShip (autosignup, agent_key/platform_key, согласие с офертой через UI)
- [ ] ЗАРАНЕЕ: консультация юриста по 152-ФЗ (согласие + APIShip как обработчик)

### M4 — Auth & Account (backlog from 2026-06-25 session)
- [ ] Verification banner must show on ALL dashboard tabs, not just /dashboard
- [ ] Graceful registration: account must be created even if email send
      fails; user lands on /verify-email with resend option (currently
      returns 500 on send failure)
- [ ] Password reset via token link (NOT password-in-email — security/152-ФЗ)
- [x] Rate limiting on password reset endpoint — реализовано, см. блок «Безопасность» (P0-SEC10 остаток · 49e9bdb)
- [ ] User settings page (profile, change password, company data)
- [x] Login lockout after N failed attempts (brute-force protection) — реализовано, P0-SEC4 (`auth/login`, бакет `login`, 5 попыток / 15 минут)
- [ ] Optional 2FA (architecture should allow it)
- [ ] Support widget / contact form + support workspace
- [ ] Email notifications for key events (order created, delivery status)
- [ ] Account activity log (security + support)
- [ ] In-cabinet tariffs/billing page
- [ ] Session management (logout all devices)
- [ ] Cookie consent banner + Privacy Policy page (152-ФЗ)
- [ ] DB backups on production VPS (152-ФЗ data preservation duty)
- [ ] Monitoring & alerts for service downtime

### M4 — Security & compliance (audit backlog from 2026-06-26)

_Источник: OCO_Deep_Audit_2026-06-26.md_

**Блокеры до приёма реальных пользователей:**
- [ ] Хостинг РФ выбран и задокументирован; заполнить раздел 5.7 Политики ПДн
- [ ] HTTPS + HSTS (`Strict-Transport-Security`) на проде
- [ ] Бэкапы БД целиком (не только настроек) — ежедневные, проверить восстановление
- [ ] Уведомление в РКН подано (до первых реальных пользователей); номер вписан в Политику ПДн
- [ ] Согласия по 152-ФЗ в форме регистрации разделены: ПДн пользователя / передача каждому обработчику / автосписание — не один чекбокс
- [ ] Cookie-баннер реализован и опубликован вместе с Политикой ПДн
- [ ] Механизм удаления / обезличивания ПДн получателя по запросу (право на удаление, 152-ФЗ)
- [x] P0-SEC1 · ef74667 · Починить `settings/restore`: не доверять `apishipPasswordEnc` из файла; проверять расшифровку или требовать повторного ввода пароля APIShip (ранняя частичная правка: 7cec9dc)

**Безопасность — до или сразу после запуска:**
- [x] P0-SEC12 · 05dd1c0 · Шифрование `recipientName` / `recipientPhone` / `destAddress` на уровне приложения (как `apishipPasswordEnc`, отдельный `RECIPIENT_PII_ENCRYPTION_KEY`; в скоуп также включён `destAddress` — сверх исходной формулировки только name/phone)
- [x] P0-SEC3 · 7f5575a · Ужесточить CSP: убрать `unsafe-eval`, перейти на nonce (Next.js 15 поддерживает)
- [x] P0-SEC2 · fb45d57 · CSRF-защита: проверка заголовка `Origin` на всех мутирующих роутах (POST/PATCH/DELETE)
- [x] P0-SEC4 · fa24e27, 8592530 · Rate-limit перенесён из памяти процесса в Postgres (`RateLimitBucket`, атомарный upsert через `$executeRaw`, единый `getClientIp()` helper). Nginx-требование к `X-Forwarded-For` остаётся открытым — см. отдельный пункт ниже.
- [ ] Лимит размера тела запроса на всех роутах (особенно `restore`) — ЧАСТИЧНО: лимит 64KB на `settings/restore` реализован (P0-SEC1 · ef74667). Остальные мутирующие роуты — не в скоупе, отдельная задача.
- [x] P0-SEC5 · 797f486, 66bf718 · AuditLog — запись ключевых событий: вход (успех/неудача), смена пароля, запрос/подтверждение сброса пароля, создание отправления, экспорт CSV, обезличивание, восстановление настроек. `companyId` добавлен в схему для сегментации по продавцам. Список отправлений (`GET /api/shipments`) сознательно не логируется — см. ADR.
- [x] P0-SEC6 · 00c4d10 · `npm audit` — 2 умеренные (CVSS 6.1, GHSA-qx2v-qp2m-jg93, вложенный `postcss` внутри `next`) закрыты через `overrides` в корневом `package.json`; `next` обновлён до последнего патча 15.5.20; `engines.node` + `.nvmrc` зафиксированы. См. ADR.
- [ ] Трекер ошибок (GlitchTip или Sentry-совместимый) — поднять до первых клиентов
- [ ] P0-SEC14 · `TariffQuote.rawResponse` и `TrackingEvent.rawResponse` хранят полный JSON-ответ APIShip в открытом виде — вероятно, включает `destAddress` из тела calculate/create-запроса. Периметр не закрыт P0-SEC12 (найдено при инвентаризации P0-SEC5). Требует отдельного решения: шифровать целиком / редактировать перед записью / не хранить raw.
- [ ] Nginx должен явно перезаписывать `X-Forwarded-For` (не доверять клиентскому значению) — требование к деплою на хостинге. До этого IP-based rate-limit (все 6 бакетов) обходится подменой заголовка. Зафиксировать в деплой-инструкции P1-HOST.
- [x] P0-SEC10 (остаток) · 49e9bdb · Rate-limit на `/api/auth/reset-password` добавлен — 6-й bucket (`reset-password`, 5 попыток/15 мин, ключ IP-only), по тому же паттерну, что и остальные 5 (P0-SEC4). Токен 256 бит, брутфорс маловероятен, но соответствует P0-SEC10 из мастер-плана.
- [ ] Rate-limit на `/api/user/password` и `/api/auth/verify-email` — отдельная задача (отложено при P0-SEC4). `user/password` защищён сессией, `verify-email` — токеном; не критично для MVP.
- [ ] Повторный `npm audit` перед каждым деплоем и периодически в разработке — разовая проверка (P0-SEC6) не гарантирует отсутствие новых CVE в будущем; пока без автоматизации (CI/Dependabot), см. ADR.

**Оптимизация кода — малая кровь:**
- [x] P0-SEC7 · 519ed4f, 87b6c73, c29b10a, 7a1ebdc, 98b5e06 · Хелпер `withAuth(handler)` централизует проверку авторизации — переведено 19 из 19 запланированных роутов (пилот + 4 партии), опция `requireEmailVerified` заменяет 3 дублировавшиеся ручные проверки. `/api/auth/send-verification` сознательно не переведён — см. отдельный пункт ниже.
- [x] P0-SEC8 · 237bdc8 · Индекс `companyId + createdAt` на таблице `Shipment` — покрывает список отправлений и CSV-экспорт (сортировка по `createdAt` в рамках `companyId`). Индекс под дашборд (`companyId + status`) уже существовал отдельно.
- [x] P0-SEC9 · 237bdc8 · Убрать мёртвое поле `apishipKeyRef` из схемы Prisma — ноль обращений в коде, 0 непустых legacy-значений в БД перед удалением.
- [x] P0-SEC13 · ffe344c · Baseline-миграция Prisma — история миграций синхронизирована с фактическим состоянием БД (ранее применялась через `db push`, `migrate dev` падал на shadow DB drift). Один baseline-файл + приёмочный тест (добавление/откат тестового поля через `migrate dev`) подтвердил: работает без ошибок shadow DB и checksum.
- [x] P0-SEC7 (остаток) · 5556500 · `/api/auth/send-verification` переведён на `getCurrentUser()` (не `withAuth` — обёртка проверяла бы авторизацию раньше rate-limit, что нарушило бы защиту от спама неавторизованными запросами). Порядок «rate-limit → auth» сохранён вручную. Побочный эффект: сверка `companyId` между сессией и БД теперь есть (раньше отсутствовала); edge-case «пользователь удалён, JWT ещё валиден» теперь отвечает 401 вместо прежнего 404 — унификация с остальными 19 роутами.

**UX — до запуска:**
- [ ] Онбординг-чеклист на пустых экранах дашборда и списка отправлений (US-5.3)
- [ ] Инструкция «как получить доступ к APIShip» прямо в форме подключения
- [ ] Показывать «сколько сэкономлено» уже на экране расчёта (до создания заказа)
- [ ] Гладкий resend верификационного письма — заметная кнопка, отсутствие тупика если письмо не пришло
- [ ] Страница `/settings` (адрес отправителя + резервная копия) не связана с навигацией `/dashboard/settings` — продавец не может туда попасть, кроме прямого ввода URL. Решить: перенести `CompanySettingsForm`/backup-панель во вкладки `/dashboard/settings`, либо добавить ссылку в сайдбар.
- [ ] Поля «Логин/Пароль APIShip» в UI (`/settings`, `/dashboard/settings`) — временный артефакт исполнения через APIShip per-company (унаследовано из v1, риск R1). Формально противоречит красной линии модели F, но реальной экспозиции нет — реальных продавцов ещё нет. Закрывается сам собой при готовности P0-EXE1 (прямой адаптер СДЭК) — форма подключения APIShip заменяется формой подключения СДЭК. Отдельного действия сейчас не требуется; не удалять раньше EXE1 — сломает создание отправлений без замены.

**Готово, когда:** пройден чек-лист «можно пускать первого клиента».

## M5 · После запуска (backlog)
Цель: доработки и перевозчик-специфичные возможности, отложенные до стабильного M4.
Не начинаем, пока M4 не проверен руками и пилоты не работают.

- Автоматический расчёт объёма отгрузки
  - Объём вычисляется автоматически из габаритов мест (длина × ширина × высота), не вводится вручную.
  - Для MVP — сумма объёмов мест (коробок), а не товаров. Учёт упаковки и пустот — отдельно, позже.
  - Контекст: некоторые перевозчики (например, Яндекс.Доставка) требуют объём в м³ при создании отгрузки.

- Поле фото товара (photoUrl) и проброс фото перевозчику
  - Добавить опциональное поле photoUrl у товара (публичная ссылка на изображение, валидный URL до 2048 символов).
  - Проброс фото — перевозчик-специфичная опция. На текущий момент в документации APIShip передача фото описана только для Яндекс.Доставки (поле item.photoLinks → photo_links, только тарифы «Доставка в другой день», только при создании заявки, не при редактировании, не для Cargo/Express).
  - Для других перевозчиков поддержку проверять индивидуально при подключении.

- Слой перевозчик-специфичных полей при создании отправления
  - Универсальная форма создания отправления не покрывает все случаи: у каждого перевозчика свои дополнительные поля и опции (у Яндекса — планирование отгрузки, объём/вес отгрузки, опция «нужна помощь с погрузкой», повтор по дням недели; передача фото; и т.п.).
  - Нужен слой, который добавляет перевозчик-специфичные поля поверх базовой формы.
  - Это архитектурное решение, проектировать отдельно после MVP.

---

## Что параллельно (не код, но критично)
С первого дня — интервью с брендами (30–50) и подбор 5 пилотных. По документу это важнее кода:
код сегодня дешёвый, а первые бренды и их данные — нет.

---

## После MVP — направление развития (Phase 2)
НЕ строим сейчас; это записано, чтобы строить MVP с прицелом на будущее (см. docs/DECISION_ENGINE.md).
Браться за это — только после того, как MVP подтверждён на пилотных брендах.
- Правила автовыбора перевозчика по условиям (вес, габариты, тип доставки, регион, категория) —
  с интерфейсом настройки для продавца.
- Автоматический режим без ручного выбора для крупных продавцов (сотни заказов в день):
  пакетная обработка, фоновая очередь, идемпотентность, учёт лимитов APIShip.
- API для продавцов и массовый импорт заказов — два разных канала: (а) собственный сайт/магазин
  продавца (Tilda, InSales, свой движок и т.п.), который передаёт заказ в OCO через API/вебхук без
  ручного ввода; (б) интеграции с маркетплейсами и массовый CSV-импорт. Для MVP продавец всегда
  вводит адрес сам, вручную, в кабинете — на основе того, что покупатель указал в его канале продаж.
- Публичная страница/ссылка для покупателя (без входа в систему), где он сам вводит свой адрес
  и желаемое время доставки, минуя продавца. Снижает ручной труд при больших объёмах, но требует
  отдельной защищённой публичной формы, защиты от спама и канала доставки ссылки (SMS/мессенджер) —
  это отдельный продуктовый кусок, не часть логистического MVP.
- Расширенное использование полей APIShip (страховка, объявленная ценность, наличный расчёт и др.).
- Коэффициенты второго уровня: качество по региону и категории, стабильность срока,
  доля повреждений/потерь, штраф за просрочку.
