# Вопросы перевозчикам — что спросили, что ответили

**Зачем файл.** Вопросы к перевозчикам возникали и раньше, но жили внутри того
исследовательского файла, где случились. Из-за этого нельзя было ответить на два простых
вопроса: **что уже отправлено** и **что уже отвечено**. Здесь они отслеживаются вместе.

**Что здесь НЕ лежит.** Вопросы к юристу — они в `docs/ROADMAP.md` пункт 21 и в
`docs/master-plan.md` §4. Задачи, требующие живого аккаунта, — в
`docs/LIVE_ENVIRONMENT_TASKS.md`. Замеры — в `docs/research/`. Здесь только переписка с
перевозчиком и её состояние.

**Как писать вопрос.** Коротко, фактически, без OCO: Юлия обращается как **ИП, встраивающий
доставку в собственные системы**. Перевозчику не нужно знать про платформу, чтобы ответить
про своё API, а лишняя рамка порождает встречные вопросы вместо ответа.

**Формат записи.** Каждый вопрос несёт: текст **как он уходит**; чем вызван, со ссылкой на
замер или файл; дату отправки или «не отправлен»; ответ и его дату или «ответа нет»; и что
он решает или блокирует.

---

## УЖЕ ОТВЕЧЕНО В ДРУГОМ МЕСТЕ — не спрашивать заново

Два вопроса из ранних списков закрыты замерами и здесь не заводятся. Записаны, чтобы их не
задали повторно.

- **Проверяются ли пределы класса на этапе расчёта?** — **НЕТ, измерено.** Посылка 15 кг
  получила офферы класса `courier`, у которого документированный предел 10 кг. Источник:
  `packages/core/src/carrier-adapter/yandex/express-taxi-class-limits.ts:22–23` и
  `docs/DECISIONS.md:1815–1816`. Наш собственный фильтр построен именно на этом.
- **Можно ли получить офферы нескольких классов одним вызовом?** — **НЕТ, измерено.** При
  `taxi_classes: ["express","courier"]` возвращаются офферы только класса `courier`.
  Источник: `docs/DECISIONS.md:1811–1812`.

---

# ЯНДЕКС

## Отправлено 27.08.2026, ответа нет

Оба ушли через поддержку, раздел «Работа с API».

### Я-1. Входит ли НДС в `pricing_total`

**Текст:** «В ответе `offers/create` поле `offer_details.pricing_total` содержит стоимость
доставки. Включает ли эта сумма НДС, или указана без него?»

**Чем вызван.** Цена из этого поля попадает на карточку продавцу без единого преобразования.
У соседнего семейства (Экспресс) net и gross приходят разными полями и различаются ровно на
22 %, а здесь — одно число без метки, и второго поля для сравнения нет. Если окажется, что
это gross, подпись «дешевле» будет вставать не на ту карточку и молча. Подробности и замеры:
`docs/OFFER_BADGES.md` §3, подраздел «Оговорка, без которой обе ценовые подписи могут быть
неверны: база НДС», и `docs/LIVE_ENVIRONMENT_TASKS.md` задача 14.

**Ответ:** ответа нет.

**Что решает.** Правильность обеих ценовых подписей на экране офферов, а также отчёта о
переплате, рейтинга перевозчиков и статистики по направлениям — все три считаются из этих
цен. Это **самый дешёвый** способ закрыть вопрос: альтернатива — сверка с закрывающими
документами по реальному заказу.

### Я-2. Держится ли потолок объявленной стоимости 250 000 ₽ на боевом контуре

**Текст:** «В FAQ указан потолок объявленной стоимости 250 000 ₽. На тестовом контуре запрос
с объявленной стоимостью 3 000 000 ₽ был принят и вернул расчёт. Действует ли ограничение на
боевом контуре, и что возвращается при превышении?»

**Чем вызван.** Замер на tst: приняты 3 000 000 ₽ при документированном потолке 250 000 ₽
(`docs/research/yandex-delivery-api-2026-07-10.md:78–79`). Запись там заканчивается словами
«на бою отвергнет» — это **предсказание, а не замер**.

**Ответ:** ответа нет.

**Что решает.** Нужна ли нам собственная валидация объявленной стоимости и что показывать
продавцу при превышении. Объявленная стоимость меняет цену нелинейно, поэтому молчаливое
обрезание значения было бы худшим из исходов.

## Не отправлены

### Я-3. Тариф «Грузовой» не возвращает офферы

**Текст:** «Аккаунт подключён к API Яндекс Доставки. Метод `tariffs` для Москвы возвращает
тариф „Грузовой“ как доступный, с минимальной ценой 1007 ₽ и списком кузовов S / M / L. При
этом `offers/calculate` с `taxi_classes: ["cargo"]` возвращает пустой список при любых
параметрах: с `cargo_type` (van, lcv_m, lcv_xl) и без него, для груза 200 кг габаритами
1,20×0,80×0,80 м по Москве, при `cargo_loaders` 0, 1 и 2. HTTP 200, тело `{"offers": []}`,
без текста ошибки. На том же маршруте `taxi_classes: ["express"]` возвращает пять офферов.
Подключён ли аккаунт к тарифу „Грузовой“? Требуется ли отдельное соглашение или настройка?
При каких условиях `offers/calculate` возвращает по нему офферы?»

**Чем вызван.** Замер от 31.07.2026, **нами не переснимался**. В реестре перевозчиков вариант
`cargo` помечен `applicable: false` с причиной «тарификация по объёму кузова (S/M/L/XL/XXL),
не по фиксированному весу» — то есть мы уже знаем, почему не котируем его сами, но не знаем,
почему API молчит.

**Ответ:** не отправлен.

**Что решает.** Возможен ли для нас грузовой сегмент вообще. Пустой ответ без ошибки
неотличим от «нет вариантов», поэтому без ответа перевозчика диагностировать нечего.

### Я-4. Как получить боевой `platform_station_id`

**Текст:** «`POST /api/b2b/platform/warehouses/list` с телом `{"filter": {}}` возвращает
`{"warehouses": []}` — у аккаунта нет ни одного склада. Без `platform_station_id` невозможно
создать заявку в семействе „Доставка в другой день“. Как получить боевой
`platform_station_id`? В документации сказано, что он выдаётся по запросу коммерческим
менеджером; метод `warehouses/create` помечен `[*Deprecated] No longer supported` без указания
замены. Какой путь правильный сегодня?»

**Чем вызван.** Пустой `warehouses/list` — блокирующее условие живой проверки Экспресса
(`docs/LIVE_ENVIRONMENT_TASKS.md` задача 12). Замер от 31.07, **в репозитории как измерение
не записан**.

**Ответ:** не отправлен.

**Что решает.** Без точки отправления живая проверка Экспресса невозможна в принципе.

**Отдельно, но НЕ этим вопросом:** можно ли вместо своего склада использовать идентификатор
опубликованного сортировочного центра — это задача 13 в `LIVE_ENVIRONMENT_TASKS.md`, и она
проверяется чтением, без перевозчика.

### Я-5. Чем `courier` отличается от `express` на практике

**Текст:** «Для одного отправления (1 кг, 0,30×0,20×0,10 м, Москва) `offers/calculate`
возвращает для `taxi_classes: ["courier"]` и `["express"]` практически одинаковые цены
(349 / 331 / 311 / 314 / 293 против 349 / 332 / 311 / 314 / 293), причём офферы класса
`courier` несут те же значения `description` (`express`, `express_30min_longer`,
`2_hours_delivery` и другие). Чем классы различаются для отправителя, если цена и описания
совпадают? Различается ли транспорт исполнителя фактически?»

**Чем вызван.** Замер от 31.07 на боевом контуре. Мы держим `courier` и `express` как две
отдельные услуги с разными пределами, и если разницы нет, одна из них лишняя.

**Ответ:** не отправлен.

**ВАЖНО при перепроверке.** Этот вопрос **нельзя** проверять на песочнице: тестовый контур
подделывает `taxi_class` — возвращает один и тот же набор офферов, переклеивая класс. См.
`docs/CARRIERS.md` §3a.

**Что решает.** Нужны ли нам обе записи в реестре услуг, и что писать продавцу об их отличии.

### Я-6. Переносит ли `offer_payload` запрошенные `cargo_options`

**Текст:** «Документация помещает `cargo_options` и в `requirements` метода
`offers/calculate`, и в `client_requirements` метода `claims/create`, но не говорит,
переносит ли `offer_payload` запрошенные опции автоматически. Нужно ли повторять
`client_requirements` при создании заявки, если опции уже указаны при расчёте? Что произойдёт,
если они указаны при расчёте, но не переданы при создании, или если различаются?»

**Чем вызван.** У нас есть флаг термосумки, который продавец ставит до расчёта. Если опция не
переносится, заявка создастся без неё молча.

**Ответ:** не отправлен.

**Что решает.** Правильность создания заявки с термосумкой — то есть будет ли в машине то,
за что продавец заплатил.

### Я-7. Как подтвердить, что требование действительно учтено

**Текст:** «Документировано, что заявка может быть создана с проигнорированным требованием, и
признаком служит предупреждение `requirement_unavailable`. Есть ли способ узнать фактический
состав требований по созданной заявке? В ответе `claims/info` поле `client_requirements`
повторяет запрошенное, а `matched_cars` содержит `taxi_class`, `cargo_type`, `cargo_loaders`,
но не содержит `cargo_options`.»

**Чем вызван.** Наблюдение 31.07 по составу ответа. Мы читаем коды предупреждений и храним их
как нейтральное перечисление (`docs/DECISIONS.md:1835`), но подтвердить наличие термосумки
нечем.

**Ответ:** не отправлен.

**Что решает.** Можем ли мы вообще сказать продавцу «термосумка будет» — или только
«запрошена».

### Я-8. Совпадают ли `warnings` в двух ответах

**Текст:** «Поле `warnings` документировано и в ответе `claims/create`, и в ответе
`claims/info`. Совпадают ли эти наборы? Может ли предупреждение присутствовать в одном ответе
и отсутствовать в другом?»

**Чем вызван.** Наш код **уже исходит из того, что могут различаться**: `mergeClaimWarnings`
объявлена в `packages/core/src/carrier-adapter/yandex/parse-claim-warnings.ts:51`, а
вызывается в `express-client.ts:848` — и объединяет оба набора. Это допущение, а не
измеренный факт.

**Ответ:** не отправлен.

**Что решает.** Нужно ли объединение вообще, и не теряем ли мы предупреждение, читая только
один ответ.

### Я-9. Справочник сроков по городам

**Текст:** «Существует ли справочник ориентировочных сроков доставки по городам, или
единственный источник — интервалы в конкретном оффере?»

**Чем вызван.** Замер 31.07: Москва → Москва забор 01.08, доставка 02.08; Москва →
Санкт-Петербург забор 01.08, доставка 03.08. Записано в `docs/DECISIONS.md:67`.

**ЧАСТИЧНО УЖЕ ОТВЕЧЕНО, и спрашивать надо только остаток.** Метод, возвращающий срок до
полного расчёта, **существует**: `pricing-calculator` отдаёт `delivery_days` и не требует ни
получателя, ни цен товаров (`docs/research/yandex-delivery-api-2026-07-10.md:12–18`). Открыт
только вопрос про справочник по городам.

**Ответ:** не отправлен.

**Что решает.** Можем ли показывать ориентировочный срок до того, как продавец нажал
«Рассчитать».

### Я-10. Тариф «На маркетплейсы»

**Текст:** «На сайте и в приложении есть тариф „На маркетплейсы“. В документации API мы его
не нашли. Каким методом он обслуживается и доступен ли он через API?»

**Чем вызван.** Наблюдение 31.07. В репозитории про этот тариф нет ничего.

**Ответ:** не отправлен.

**Что решает.** Есть ли у нас слепое пятно в покрытии услуг перевозчика.

### Я-11. Поле `photo_links`

**Текст:** «Сторонние интеграции передают в заявку „Доставки в другой день“ поле
`photo_links` со ссылками на фотографии товаров и указывают, что при редактировании заявки
фотографии не передаются. Для чего используется это поле, обязательно ли оно и что оно даёт
отправителю?»

**Чем вызван.** Наблюдение 31.07. В `docs/ROADMAP.md` (M5) висит **задача** добавить поле
фото и пробрасывать его — но что это поле даёт, не знает никто.

**Ответ:** не отправлен.

**Что решает.** Стоит ли задача из M5 того, чтобы её делать.

### Я-12. Стоимость отмены Экспресса, кроме `cancel-info`

**Текст:** «Подтверждено ли, что отмена подтверждённой заявки бесплатна до прибытия курьера к
отправителю, платна после прибытия и невозможна без поддержки после передачи груза? Есть ли
способ определить текущую стоимость отмены программно, кроме `claims/cancel-info`?»

**Чем вызван.** Границы **прочитаны в документации, но не измерены**: ни одна заявка не
проходила дальше `accept`, поэтому саму границу никто не наблюдал
(`docs/LIVE_ENVIRONMENT_TASKS.md` задача 12).

**Ответ:** не отправлен.

**Что решает.** Что показывать продавцу до подтверждения, чтобы платная отмена не стала
сюрпризом. Первая половина вопроса может закрыться живой проверкой; вторая — только ответом
перевозчика.

### Я-13. Обязателен ли Календарь отгрузок для получения офферов

**Текст:** «Обязателен ли настроенный Календарь отгрузок для того, чтобы `offers/create`
возвращал варианты доставки, или он влияет только на фактический забор со склада? Мы
понимаем, что календарь относится к отгрузке курьером со склада, а привоз в ПВЗ и в
сортировочный центр его не требуют, — но хотим убедиться, что незаполненный календарь не
приводит к пустому ответу при расчёте.»

**Чем вызван.** Гипотеза 31.07. Пустой ответ при расчёте неотличим от «нет вариантов», а
причин у него может быть несколько.

**Ответ:** не отправлен.

**Что решает.** Одну из возможных причин пустой выдачи — то есть время на диагностику.

### Я-14. Может ли один аккаунт работать с обоими семействами

**Текст:** «16.07 поддержка сообщила, что аккаунт можно интегрировать только с одним типом —
„Экспресс“ или „Доставка в другой день“. Относится ли это ограничение к настройке кабинета
или к API? Может ли один аккаунт одновременно использовать оба семейства методов через API?»

**Чем вызван.** **Два замера с разницей в месяц говорят разное** — 31.07 один токен отработал
против обоих хостов, 27.08 одно семейство ответило 403, пока второе авторизовалось. Полностью
описано в `docs/CARRIERS.md` §1; здесь не повторяется.

**Ответ:** не отправлен.

**Что решает.** Нужен ли нам один аккаунт или два, и как объяснять это продавцу при
подключении. Сегодня недоказаны оба варианта.

---

# СДЭК

## ЗАКРЫТЫЙ ЦИКЛ — вопрос отправлен, ответ получен, решение принято

Единственная полностью замкнутая переписка на сегодня. Записана в том числе как образец: у
вопроса есть дата отправки, у ответа — дата, и есть решение, которое из ответа выросло.

### С-1. Объявленная стоимость и сбор в калькуляторе

**Отправлено 14.08.2026** менеджеру, в составе четырёх вопросов
(`docs/research/cdek-production-calculator-2026-08-14.md` §7, вопрос 1): «Услуга
„Дополнительный сбор за объявленную стоимость“ по договору отключена — или страхование входит
в тариф? Можно ли подключить? Порог 3000 ₽ — правило отдельных тарифов, где список?»

**Ответ получен, и в двух частях.**

- **18.08.2026 — от СДЭК напрямую**, по обеим половинам вопроса. Зафиксировано в
  `docs/DECISIONS.md`, запись «2026-08-14 · CDEK calculator `services` depends on the CONTRACT
  TYPE»: «CDEK answered both halves directly on 18.08, and the production contour agrees with
  the answer.»
- **От интегратора СДЭК** — что объявленная стоимость передаётся в калькулятор как
  `services: [{code:"INSURANCE", parameter:"<стоимость>"}]`, с примером на методе
  `/v2/calculator/tariff` (потарифном), а не на `tariffAndService`.
  **Получен 24.08.2026** и в тот же день стал причиной повторного прогона
  (`cdek-production-calculator-2026-08-24.md:14–19`).

**Что из этого вышло.** Прогон 24.08 разделил три оси, по которым пример интегратора
отличался от нашего замера: другой МЕТОД, другая ВЕЛИЧИНА (5000 против 1000, выше и ниже
порога 3000) и другой ДОГОВОР (`vat_rate` 5 против нашего 22). Правило «для типа 1 не
отправлять `services`» осталось в силе: передача этого поля роняет весь список — 38 строк из
38. Замеры: `cdek-production-calculator-2026-08-24.md`.

## Отправлено 14.08.2026, ответа нет

### С-2. Застрахована ли посылка, если услуга сбора недоступна

**Текст (как ушёл):** «Если услуга „Дополнительный сбор за объявленную стоимость“ по договору
недоступна — застрахована ли посылка на объявленную стоимость, и что возмещается при утере?»

**Чем вызван.** На боевом контуре 37 строк из 38 ответили
`ve_additional_service_unavailable` — услуга на договоре не включена вовсе. Мы трактуем этот
код как «сбор к этой посылке не применяется» и оставляем тариф в выдаче. Но что при этом
происходит со страхованием, из API не следует:
`cdek-production-calculator-2026-08-14.md:156–157` — «**НЕ ИЗМЕРЕНО: страхуется ли посылка
вообще** при недоступной услуге и что возмещается при утере. Это вопрос к перевозчику, а не к
API».

**Ответ:** ответа нет.

**Что решает.** Что мы вправе говорить продавцу о сохранности груза. Сегодня — ничего, и это
единственная честная позиция.

### С-3. Сдача в пункт приёма и печатные документы

**Текст (как ушёл):** «Сдача в пункт приёма: в какой офис можно привозить отправление и нужны
ли при этом печатные документы?»

**Чем вызван.** Вопрос 3 из четырёх, отправленных 14.08. У СДЭК колонка «Этикетка» в кабинете
показывает «Пока недоступна», печатные формы не построены, и неизвестно, обязателен ли
бумажный документ при сдаче в офис.

**Ответ:** ответа нет.

**Что решает.** Можно ли вообще сдать заказ без печати — то есть является ли отсутствие
печатных форм блокирующим для продавца или только неудобным. От этого зависит срочность
задачи 7 в `docs/LIVE_ENVIRONMENT_TASKS.md`.

## Отвечено не перевозчиком, а замером

### С-4. Поломка `tariffAndService` на `api.edu.cdek.ru`

Вопрос 4 из четырёх, отправленных 14.08: «`tariffAndService` на `api.edu.cdek.ru` — известная
ли это поломка?» **Ответа от перевозчика нет**, но 26.08 оба калькулятора ответили 200, и
детерминированный 500 больше не воспроизводится
(`docs/research/cdek-tariff-limits-edu-2026-08-26.md` §2).

**Починили ли песочницу или сбой был долгим, но временным, — НЕ ИЗМЕРЕНО.** Вопрос можно
снять с отправки: практического значения он больше не имеет.

---

## Как поддерживать

**Запись меняется только вместе с состоянием переписки:** отправили — ставим дату; пришёл
ответ — записываем его и дату; из ответа выросло решение — ссылка на `DECISIONS.md`.

**Ответ, полученный от перевозчика, не является измерением.** С-1 показывает, зачем это
различать: ответ интегратора отличался от нашего замера по трём осям сразу, и понадобился
отдельный прогон, чтобы понять, какая из них важна. Ответ говорит, как **должно** быть; замер
— как есть.

**Вопрос, закрытый замером, а не ответом, остаётся здесь** с пометкой об этом — см. С-4.
Иначе следующий человек отправит его повторно.
